# 更新日志

本文件记录 QQ 群聊管理插件的主要变更。

## 2.20.32 - 2026-08-27

- 修复入群欢迎在 QQ 原生白名单、QQ 客户端或其他管理员外部审批后不生效的问题：对已配置欢迎的群低频缓存近期申请，并在成员首次发言时补发一次，带并发去重和过期清理。
- 保留欢迎规则的群选择范围；未覆盖的群不会误发。补发链路不轮询成员列表，兼容 AstrBot QQ 适配器未提供成员加入事件的限制。
- 全局 AI/OCR 配置继续只存储在顶层，清理旧版群策略中的重复副本；更新测试和文档说明。

## 2.20.31 - 2026-08-26

- 优化 B 站推送：视频优先展示视频标题与简介，普通动态展示截断后的简短说明，直播使用直播标题；图片卡片改为等比容纳封面，避免竖图被裁切。
- 图片卡片底部和消息正文按类型提供可点击的文字链接（查看视频、查看原动态、进入直播间）；富媒体不可用时继续降级为 Markdown 命名链接。

## 2.20.30 - 2026-08-26

- 按钮卡发送遇到超时、连接中断或空异常时不再盲目补发文字提示，避免 QQ 已接收卡片后出现两条真人验证消息；只有明确的权限/参数拒绝才使用文字兜底。

## 2.20.29 - 2026-08-26

- 串行化真人验证题创建，修复入群审批和首次发言同时触发时重复发送验证卡/文字提示的问题。
- 全局 AI 二次确认支持 3 个有序回退模型，主确认模型异常、超时、无效或不支持图片时按顺序继续尝试，并沿用总超时与去重校验。
- WebUI、配置 schema、运行时迁移和配置快照同步支持确认回退列表。

## 2.20.28 - 2026-08-26

- 真人验证正常发送时只发送一张带按钮的验证卡；仅在 QQ 按钮/Markdown 不可用时发送文字兜底，避免同一题显示两次。
- 验证消息支持按全局策略选择覆盖群、完成或超时自动撤回及自定义超时时间；验证超时后下一次发言会重新出题，验证成功会清理旧题。
- AI 审核总超时现在包含图片预处理；只有明确的视觉能力不支持错误才降级为文字审核，普通图片传输错误不会误走文字降级。
- QQ 原生语音附件的 `asr_refer_text` 进入关键词审核、AI 审核和违规记录。

## 2.20.27 - 2026-08-26

- 调整真人验证按钮卡的单行文案顺序，将验证用途、按钮/纯数字操作方式和未完成验证的处理说明放在算式之后，兼容只显示键盘卡末尾内容的 QQ 客户端。
- QQ 原生语音附件的 `asr_refer_text` 现在会进入关键词审核、AI 审核和违规记录，避免语音转写内容绕过群消息审查。

## 2.20.26 - 2026-08-26

- 图片 AI 审核会读取 AstrBot 提供商的输入模态；明确不支持图片的文本模型不再静默参与图片判定，改由视觉回退模型继续审核，避免 DeepSeek 等文本模型误把图片判为违规。未声明能力的旧版提供商保持兼容。

## 2.20.25 - 2026-08-26

- 真人验证按钮卡改为单行完整提示，并与随后文字说明使用不同内容；兼容只显示键盘卡最后一行或折叠重复消息的 QQ 客户端，仍支持直接发送纯数字答案。

## 2.20.24 - 2026-08-26

- 修复旧版或精简 WebUI 只保存轮询间隔时把全局入群拒绝、消息/图片关键词、媒体处置回复和关键词冷却重置为默认值的问题；运行配置接口现在按请求字段进行部分更新，已有入群审核配置不会因保存间隔被覆盖。

## 2.20.23 - 2026-08-26

- 真人验证的文字提示和按钮卡使用不同的 QQ 消息序号，避免客户端或官方接口去重后只显示算式。

## 2.20.22 - 2026-08-26

- 配置快照增加顶层 AI/OCR、入群审核轮询和 B 站轮询设置；插件配置重建或首次部署时恢复仍为默认值的设置，同时保留用户已修改的值，继续排除 B 站 Cookie 等敏感凭据。

## 2.20.21 - 2026-08-26

- 复读达到阈值时会撤回该轮已记录的全部复读消息，再随机禁言参与者；不同内容仍会打断当前轮次。

## 2.20.20 - 2026-08-26

- 真人验证先发送完整文字提示，再发送按钮卡；按钮卡折叠或不可用时仍可按提示直接发送数字。
- 图片关键词 OCR 不再把同一条消息中的普通文字当作图片元数据，避免正常图片被误撤回。

## 2.20.19 - 2026-08-26

- 真人验证按钮卡和随后发送的普通文字都会显示验证用途、按钮/纯数字操作方式、算式及未完成验证的处理，避免部分 QQ 客户端只显示一道算式。
- AI 图片审核在主模型视觉失败时保留图片交给后备视觉模型，仅在最后一个候选模型失败时降级为纯文本审核。
- 全局群策略保存拒绝空或非法列表，群专用策略不再泄漏到旧版全局兜底字段；全局关键词回复、冷却和自动撤回时间纳入独立持久快照，即使没有群条目也能恢复。

## 2.20.18 - 2026-08-26

- AI 内容审核兼容严格 JSON 决策（含中英文判定、置信度和理由字段），DeepSeek 等模型不再因返回 JSON 被误判为无效并无谓切换回退模型。
- 模型异常日志在异常文本为空时也会记录明确类型或超时原因，避免只看到空错误；身份记录在当前群名为空时会正确回填已保存的历史群名，搜索和展示不再丢失。
- 修正艾特协议为 QQ 官方 `<qqbot-at-user id="..." />` 格式，避免使用已弃用的 `<@OpenID>` 导致客户端显示原始标签。

## 2.20.17 - 2026-08-26

- 真人验证提示改为独立发送且不复用触发消息 ID；按钮卡同时显示“真人验证”、按钮用法、纯数字回退和算式，避免 QQ 客户端只显示算式。

## 2.20.16 - 2026-08-26

- 真人验证先发送完整文字说明，再发送不复用原消息 ID 的单行按钮卡；兼容只显示算式或按默认序号去重的 QQ 客户端，并保留纯数字回退。
- 视觉 OCR 兼容对象和 JSON 字典两种模型响应格式，避免图片文字关键词被误判为空。

## 2.20.15 - 2026-08-26

- 真人验证按钮卡将完整提示加粗，并在键盘卡片后补发文字说明；兼容只显示粗体内容或折叠前置消息的 QQ 客户端，提示包含按钮用法、纯数字回退、算式和撤回说明。

## 2.20.14 - 2026-08-26

- WebUI 将全局 AI/OCR 设置拆为独立面板；保存、切换、增加或删除群级策略时，不再把模型、回退链、阈值和 OCR 配置复制到每套策略或意外清空。
- 全局群策略接口新增独立 `global_ai` 数据块并保留旧版扁平字段兼容，补充路由和页面回归测试。

## 2.20.13 - 2026-08-26

- UID 绑定和待真人验证记录现在保存群 OpenID 对应的历史群名；群配置被删除或重建后，身份页仍可按原群名搜索并正确展示记录。

## 2.20.12 - 2026-08-26

- 真人验证按钮卡片现在同时包含用途说明、点击按钮提示、纯数字回退方式和未完成验证的处理说明；按钮不可用时仍发送完整文字兜底。
- 可疑身份记录会保留或从 UID 绑定恢复 B 站 UID，支持按 UID 搜索；身份页待验证卡片优先显示历史群名，并补充成员/联合 OpenID。

## 2.20.11 - 2026-08-26

- 修复 WebUI 重新加载全局 AI 配置时，将暂时不可用的回退模型追加到列表末尾的问题；现在会保留用户设置的完整回退顺序，恢复可用后也不会悄悄改变优先级。

## 2.20.10 - 2026-08-26

- 兼容部分 QQ 客户端折叠带自定义按钮的 Markdown 正文：真人验证先发送清晰的用途与数字回退说明，再发送算式按钮卡；按钮或 Markdown 不可用时仍发送完整文字提示。
- 修复 QQ 艾特标签被客户端按原文显示的问题，统一使用官方 Markdown `<@OpenID>` 格式，覆盖禁言、撤回、图片审核、欢迎和关键词回复。

## 2.20.9 - 2026-08-26

- WebUI 的 AI 回退模型支持保留并调整实际尝试顺序，可用上移/下移按钮表达多回退优先级。

## 2.20.8 - 2026-08-26

- 真人验证按钮卡片现在在同一条消息中同时显示用途说明、算式和“直接发送数字”的回退提示，避免 QQ 客户端隐藏前置说明。
- AI/OCR 参数统一从顶层全局配置读取；策略页保存时同步一份配置到所有档案，群内 AI 开关也不再生成仅对单群生效的副本。

## 2.20.7 - 2026-08-26

- 兼容旧版 UID 绑定快照只有 `members` 映射的情况，身份页恢复群名显示并支持按群 OpenID 搜索。
- 身份页读取旧违规记录时，稳定补齐记录 ID 和复核字段，并通过现有节流保存机制持久化，避免重启后记录链接变化。

## 2.20.6 - 2026-08-26

- 图片审核遇到文本模型明确不支持视觉输入时，会在同一总超时预算内对同一模型重试文字审核；普通网络/配额错误仍直接进入下一个回退模型，避免重复消耗审核预算。

## 2.20.5 - 2026-08-26

- 真人验证会先发送一条普通文本提示，再发送带按钮的算式卡片，兼容 QQ 客户端折叠内嵌键盘 Markdown 正文的情况；按钮不可用时仍保留完整文字回退。

## 2.20.4 - 2026-08-26

- 欢迎规则保存会保留原规则中的旧群作用域，即使该群条目暂时未出现在群配置列表中。

## 2.20.3 - 2026-08-26

- 真人验证面板补充用途、通过条件和未完成验证的处理提示；按钮和文本答案仍保持纯数字输入。
- 图片视觉引用预处理使用独立有界预算，不再消耗主模型与多回退模型的 AI 审核总超时。
- WebUI 保存欢迎规则、全局关键词和全局策略时保留已登记但暂未绑定群的作用域；运行时仍只对已绑定群执行。
- 切换、新增或删除全局策略前先提交当前表单，避免未保存字段静默丢失。

## 2.20.2 - 2026-08-25

- 修复更新器把 `template_list` 重建为空时未恢复入群审核配置的问题；群审核、欢迎规则和全局策略分别恢复，不再互相影响。
- 启动迁移不再覆盖独立持久快照；即使插件状态 KV 损坏，也会继续读取配置快照。
- 修复配置快照并发保存可能落后一个版本的问题，保留最后一次 WebUI/群内设置。

## 2.20.1 - 2026-08-25

- 为入群审核群配置增加独立的 AstrBot 持久 KV 快照；配置文件缺失或首次重建时自动恢复群条目、欢迎规则和全局策略，主动保存为空不会被覆盖。
- 所有配置保存入口统一触发有界快照，避免插件更新替换目录时丢失入群审核设置；快照不包含 Cookie 等敏感凭据，也不增加常驻轮询任务。
- 图片视觉引用规范化异常改为单图跳过，继续执行文字审核和模型回退，避免图片转换失败把整条 AI 审核链拖到总超时。

## 2.20.0 - 2026-08-25

- 身份绑定和待验证成员状态增加固定上限，超限保留最新记录；成员查找使用索引，降低长期运行和 WebUI 查询的资源占用。
- 兼容旧集成直接修改嵌套身份映射的场景，索引未命中时按需重建，不影响正常 O(1) 查找。
- 发布说明明确连续发图与复读随机禁言由全局策略按群选择，群单独配置页仅保留基础设置。

## 2.19.0 - 2026-08-25

- 连续发图和复读随机禁言继续由全局群策略统一管理；群内设置按钮修改共享或全群策略时会自动拆出当前群专用策略，避免误改其他群。
- WebUI 保留策略按列表顺序匹配的能力，对覆盖范围重叠显示明确警告，不再错误拒绝合法的“专用策略 + 全局兜底”配置。
- AI 提供商错误响应统一提取并脱敏，回退链日志可显示实际失败原因；继续遵守主模型、多回退模型和二次确认的总超时预算。

## 2.18.0 - 2026-08-25

- 全局群策略新增消息频率限制：可按策略选择多个群，分别设置时间窗、触发条数、最多撤回数量、回复模板和艾特开关；默认关闭，管理员、白名单和命令消息不会累计。
- 频率限制使用有上限的成员消息窗口，触发后只撤回该成员最近的配置数量，避免长期运行占用过多内存。
- 视觉 OCR 在 AI 并发繁忙时立即跳过模型回退，继续使用已收集的本地 OCR 文本，不再排队拖延群消息审核。

## 2.17.2 - 2026-08-25

- 媒体 OCR/图片转换超时后，CPU 并发闸门会等底层任务真正结束再释放，避免慢任务叠加占用线程。
- AI 审核并发已满时立即走失败开放路径，不再排队到总超时；违规记录在节流窗口内增加延迟落盘，降低重启丢失风险。
- 补充 QQ `40011030` 权限错误的可读诊断，日志继续保留群名称、OpenID、平台和 trace_id。

## 2.17.1 - 2026-08-25

- 全局策略兼容旧版缺失字段：旧策略会继承顶层 AI、OCR、关键词和名单设置，不再意外关闭审核。
- AI 图片审核最多预处理 3 张图片，图片转换计入总审核超时；本地 OCR/图片转换增加单并发闸门，繁忙时跳过媒体处理并继续文字规则，降低 CPU 占用。
- 修复超长数字 UID 导致身份页报错、违规记录无法按被处置成员 OpenID 搜索，以及清除可疑成员后旧验证码仍可使用的问题。

## 2.17.0 - 2026-08-25

- 连续发图检测和复读随机禁言迁移到群级全局策略：每套策略可选择多个群，分别保存阈值、时间窗、撤回数量、随机禁言区间、回复模板和艾特开关。
- 单群编辑页和批量编辑页仅保留基础消息审核与本群规则；群内设置按钮会更新当前群匹配的全局媒体/复读策略。
- 全局连续发图、复读、文字关键词和图片关键词在已绑定群中可独立于单群消息审核开关运行；旧版单群媒体配置继续兼容读取。
- 修复新版基础群配置提交时覆盖旧媒体字段或导致保存异常的问题；旧策略在未显式修改媒体字段时可安全往返保存。
- 群内设置按钮遇到尚未覆盖全局策略的新群时，会自动创建仅覆盖该群的策略，不再返回“未匹配策略”错误；已配置全局策略后，未覆盖群不会继续套用旧单群媒体设置。
- 补充单群消息审核关闭时全局连续发图/复读仍可实际触发的事件级回归测试。

## 2.16.0 - 2026-08-25

- 全局运行配置改为支持多份群级策略档案：每份档案可选择多个群，按列表顺序匹配，空群范围覆盖全部已绑定群。
- 拆分拒绝关键词、成员黑白名单、AI 主备与二次确认、图片 OCR、撤回模板、艾特开关和关键词回复冷却/撤回时间的群范围，互不覆盖其他档案。
- 旧版顶层全局字段在首次保存策略时自动迁移为默认档案；配置 schema、WebUI 使用说明同步声明新结构。
- 旧版单例全局字段在通用配置页隐藏，仅保留兼容读取，避免与群级策略产生双入口覆盖。

## 2.15.0 - 2026-08-25

- 修复全局 AI 多回退审核的超时预算分配：初审不再提前为二次确认模型预留时间，确认模型只在初判 `BLOCK` 后使用剩余总预算。
- 增加主模型、多个回退模型和确认模型组合下的预算回归测试，避免主模型被过早取消并误触发回退链。

## 2.14.0 - 2026-08-24

- 违规记录新增稳定记录 ID 与人工复核状态：待复核、确认违规、误判；旧记录会在加载时自动迁移。
- 身份验证页新增违规状态筛选，支持在记录详情中更新复核状态；状态更新不改变 UID 违规次数，也不会自动修改审核规则。
- 违规 CSV 导出同步包含复核状态和复核时间，并沿用当前搜索与状态筛选条件。
- 增加复核状态迁移、精确更新、筛选和 WebUI 交互回归测试。

## 2.13.0 - 2026-08-24

- 全局 AI 审核对所有已绑定群生效，不再被单群“消息审查”开关短路；单群关键词、刷图和复读规则仍按各群开关执行。
- 收紧 AI 结果解析：只有模型输出开头的 `ALLOW`/`BLOCK` 或明确中文决策才会作为结论，说明文字中的决策词不会误触发撤回。
- 远程 GIF 和 QQ 标记为 GIF 的图片不再直接发送给不兼容的视觉模型；纯 GIF 消息的 `[表情]`、`[图片]` 占位文本也不会再进入文本模型，避免被误判撤回。
- 全局 AI 选择“只记录”时仍会继续匹配并发送关键词回复，不再吞掉原有群内回复。
- 增加全局 AI 生效范围、AI 误判防护和 GIF 路径回归测试。

## 2.12.2 - 2026-08-24

- 真人验证统一为纯数字输入：消息只显示算式和数字按钮，不再接受“答案”或“验证”等前缀。

## 2.12.1 - 2026-08-24

- 真人验证按钮提示改为“直接发送数字”，不再引导用户添加“答案”或“验证”等前缀。

## 2.12.0 - 2026-08-24

- 新增入群欢迎规则：支持多条消息、按已绑定群选择、启用开关、成员艾特变量和消息自动撤回。
- 自动审批和管理员按钮审批成功后发送欢迎消息；规则支持 `{at_user}`、`{user}`、`{username}`、`{group_name}`、`{group_openid}`、`{member_openid}`、`{uid}` 变量。
- 真人验证按钮不可用或客户端不显示时，验证消息改为提示直接发送数字答案；仍兼容解析 `答案:数字` 和 `验证 数字`。

## 2.11.0 - 2026-08-24

- B 站推送改为本地生成 720px 图片卡片，按头像、作者、类型、时间、标题、正文、封面和原动态入口分层；无正文动态不再显示孤立占位符。
- 远程 T2I 参数改为 AstrBot 4.27.3 官方支持项，本地卡片失败时才回退 T2I、Markdown 和纯文本；图片源仅接受 B 站官方媒体域名。
- 全局 AI 审核失败信息统一脱敏，异常中的 URL、Bearer/API token 和 `sk-*` 密钥不会写入违规记录、告警或调试日志。
- 身份验证页按数字 UID 排序，并容错旧状态中的异常时间值；首次打开身份页时才请求三类记录，刷新群管理页不再额外请求隐藏数据，公开接口也不再允许绕过分页返回全部记录。
- 修复违规记录标题、说明和 CSV 导出按钮的桌面布局，并在窄屏下改为纵向排列。

## 2.10.0 - 2026-08-24

- B 站动态和直播推送优先生成紧凑图片卡片：使用头像、作者、类型、时间、正文、封面和原动态入口的清晰层级，避免 QQ Markdown 中出现孤立横线或空白引用。
- 图片卡片渲染或 QQ 富媒体上传失败时自动回退到原有 Markdown 和纯文本发送，不阻断推送状态更新。

## 2.9.0 - 2026-08-24

- 新增管理员命令 `/上传群文件 <URL> [文件名]`，通过 QQ 官方群聊富媒体接口直接发送公开 URL 文件。
- 按文件名或 URL 扩展名自动识别图片、视频、语音和普通文件；严格校验 URL、文件名和官方错误码，不引入 OneBot 私有接口。
- QQ 原生指令面板同步包含 `/上传群文件`；继续明确不提供官方未开放的群文件列表和删除能力。

## 2.8.3 - 2026-08-24

- 按参考推送进一步收紧 B 站动态卡片：使用一级标题和简洁作者元信息，移除类型图标、分隔线及“暂无说明”等空内容占位。
- 仅在接口确实返回标题或摘要时展示正文块；无文字动态保留封面和“查看原动态”入口，避免出现空白引用区域。

## 2.8.2 - 2026-08-24

- 重新整理 B 站动态和直播推送卡片：封面置于主体前，增加类型图标和分隔层次，底部统一使用“打开动态/进入直播间”动作链接。
- 清理 B 站接口常见的 `-`、`暂无` 占位文本，并补齐图文动态嵌套封面和转发卡片解析，避免推送出现孤立横线或空标题。

## 2.8.1 - 2026-08-24

- 重做 B 站动态与直播推送为紧凑 Markdown 卡片：使用二级标题、状态图标、UP 主元信息、封面、标题/摘要和命名跳转链接。
- 从 B 站动态卡片提取封面并使用 QQ 官方公网图片 Markdown；图片转存失败时继续降级为不带图片的可读文本。

## 2.8.0 - 2026-08-24

- 修正复读检测为真正的连续消息序列：任意不同内容、图片或不参与审查的消息都会打断旧序列。
- 复读达到阈值后继续从普通参与者中随机选一人，在配置的最短/最长秒数范围内随机禁言，并在禁言成功后发送独立提示；提示支持 `{duration}` 和 `{at_user}`。
- QQ 原生指令面板同步改为遍历官方分页，发现后续页面已有托管面板时更新而不重复创建，并拒绝异常重复游标。

## 2.7.0 - 2026-08-24

- 全局 AI 内容审核新增可选的独立二次确认模型；仅在初判 `BLOCK` 后复核，二次判定 `ALLOW` 时直接放行。
- 二次确认失败、超时或输出无效时降级为只记录、不撤回；确认模型与主模型、回退模型必须不同，且所有调用共享审核总超时。
- 违规详情和 CSV 导出增加二次确认模型、判定、置信度与理由，便于核对模型分歧和故障。

## 2.6.0 - 2026-08-24

- 新增 `/同步指令面板`，通过 QQ 官方 `/v2/panels` 创建面向所有群的原生管理指令入口。
- 重复同步会按插件专属备注更新既有面板，不创建重复项；面板中的 4 条管理命令均限制为群主或管理员可点击。

## 2.5.0 - 2026-08-24

- 违规记录支持按身份页当前搜索条件导出 CSV，一次导出完整匹配结果，不需要逐页下载。
- CSV 使用 UTF-8 BOM，并对 Excel/WPS 可识别为公式的单元格前缀进行转义，避免消息内容在打开文件时被执行。

## 2.4.0 - 2026-08-24

- 身份验证页改为服务端搜索和分页：可按名称、B 站 UID、成员/联合 OpenID、群名称/OpenID、违规原因及消息内容查询，每类记录只返回当前页。
- 身份绑定、待真人验证和违规记录分别维护页码与请求状态；搜索使用 250 毫秒防抖，过期响应不会覆盖新结果。
- 每页 10/20/50 条的选择会保存在当前浏览器，解除绑定或清除可疑状态后只刷新对应列表，减少后台与浏览器的数据传输和渲染开销。

## 2.3.1 - 2026-08-24

- 修复 QQ GIF 表情传给不支持 `image/gif` 的视觉模型后反复重试并最终漏审的问题；内联 GIF 会在受控字节和像素范围内转换首帧为 PNG，转换失败时跳过图片并按文字继续保守审核。
- QQ 原生表情名称（例如 `[表情:[龙年快乐]]`）会直接参与图片关键词匹配，命中时无需等待 OCR 或视觉模型即可撤回。
- AI 总超时会为主模型和剩余回退模型预留预算，错误日志不再回显完整传输地址；黑白名单会先于待验证状态判断，黑名单优先且白名单可解除陈旧验证拦截。

## 2.3.0 - 2026-08-24

- 新增全局与单群成员黑名单/白名单，支持成员 OpenID、联合 OpenID 和已绑定的 B 站 UID；黑名单优先，白名单跳过常规消息审核，名单命中记录可在身份页查询。
- 新增独立的黑名单撤回回复与艾特开关，并支持批量编辑单群名单和处置模板；全局黑名单即使单群消息审查关闭也可执行。
- 将全局文字关键词、全局图片关键词、单群文字关键词、单群图片关键词、连续发图和复读处置拆分为独立回复模板与艾特开关；模板留空且关闭艾特时不发送提示。
- AI 内容审核默认改为“只记录，不撤回”，并在身份违规记录中保存模型、判定、置信度和理由；只有显式选择“撤回消息”才会停止并撤回，减少普通聊天和游戏截图误伤。
- 修复自定义艾特在部分 QQ 客户端显示为 `<qqbot-at-user>` 原文的问题：优先使用 QQ Markdown，权限失败时降级为不含原始标签的普通文本；`{at_user}` 变量仍可放在任意回复位置。
- 优化 B 站动态和直播推送排版，统一为标题、UP 主/类型/时间、摘要和命名链接；Markdown 失败时自动降级为可读纯文本，避免重复推送。
- WebUI 增加上述独立字段的单群、全局和批量编辑入口，并在违规详情显示实际处理动作和 AI 审核信息。

## 2.2.0 - 2026-08-23

- AI 群消息审核改为保守判定：只有明确 `BLOCK` 且达到可配置置信度阈值才撤回；普通吐槽、轻度脏话和游戏/动漫截图默认放行。
- AI 审核总超时改为 WebUI 可配置（5-120 秒），图片视觉审核默认关闭，主模型失败时继续使用有序回退模型。
- 新增全局和单群图片文字关键词审核；支持可选本地 OCR、视觉 OCR 模型、超时和单条图片数限制，OCR 失败时放行。
- 连续图片/表情触发后支持限制最多撤回的消息数量，避免把较早的正常图片一并撤回。
- 含 QQ 原生艾特的禁言成功和违规警告改用文本链发送，避免部分客户端把 `<qqbot-at-user>` 显示为字面量。
- WebUI 运行配置、群编辑、批量编辑、配置 schema、README 和测试同步更新。

## 2.1.0 - 2026-08-23

- 将 AI 内容审核统一迁移到 WebUI“运行配置”，支持主模型和最多 3 个按顺序选择的回退模型；主模型失败、超时或返回无效结果时才触发回退，整条链限制 20 秒。
- 身份验证页改为可折叠卡片，支持按名称、B 站 UID、成员/联合 OpenID 和群名称/OpenID 搜索，并提供 10/20/50 条分页。
- 新增有界违规记录：保存命中规则、处理时间、成员身份、群信息和截断后的消息内容，最多保留 2,000 条，可在身份页展开查询。
- 补充全局 AI、身份查询、违规记录和低资源运行说明；同步 README 与配置 schema。

## 2.0.0 - 2026-08-23

- 新增最近消息缓存和 `/撤回`：支持撤回本群或指定成员最近最多 50 条已收到的普通成员消息；遵循 QQ 官方 2 分钟和单条接口限制。
- 连续图片/表情刷屏改为个人窗口与跨成员纯媒体窗口分开判定，支持配置时间窗、数量和最少参与人数，普通文本会中断连续链。
- AI 审核支持在 WebUI 选择主模型和回退模型；主模型异常、超时或返回无效结果时才调用回退，主备均不可用时放行并限频记录。
- 新增 `/全体禁言` 和 `/全体解禁` 的官方能力检查提示；QQ 官方群 OpenAPI 当前没有全员禁言写入接口，不会伪装执行。
- 更新配置 schema、批量编辑、命令帮助和发布文档。

## 1.9.1 - 2026-08-21

- 将全局与单群关键词规则改为按需展开；默认折叠，并在同一编辑区只展开一条规则。
- 规则摘要实时显示名称、匹配方式和启用状态，校验失败时自动展开对应规则。

## 1.9.0 - 2026-08-21

- 将群管理、全局规则、运行配置和身份验证集中到独立 WebUI 页面。
- 全局规则可按已绑定群名称多选；单群与全局规则支持自定义名称、多关键词及 AND/OR 组合。
- 新增按群关键词回复冷却和回复自动撤回。
- 新增 B 站二维码扫码登录，Cookie 仅保存于 AstrBot 配置且不会回显。
- 将群内审核设置拆分为紧凑的审核条件、消息审查、关键词回复和 B 站推送子菜单。

## 1.8.0 - 2026-08-20

- 新增可限定覆盖群的全局关键词回复。
- 新增单群关键词回复及独立 WebUI 规则编辑器。
- 支持包含关键词、完全匹配、规则启停和顺序优先级。

## 1.7.2 - 2026-08-19

- 补充 AstrBot Cloud 可读取的标准 `CHANGELOG.md`。
- 完善发布材料，统一插件说明与当前功能。

## 1.7.0 - 2026-08-19

- 新增群消息关键词、AI、连续发图和复读审查。
- 新增真人验证、B 站 UID 身份绑定与唯一性校验。
- 新增 B 站动态、开播和下播推送。
- 新增群消息撤回、随机成员禁言和可配置禁言区间。

## 1.6.1 - 2026-08-19

- 增加入群申请权限诊断日志和按钮失败反馈。

## 1.6.0 - 2026-08-19

- 新增 WebUI 多群选择、批量编辑和批量应用。
- 新增审核命令和设置面板自动撤回开关。
- 改进禁言回复模板及 QQ 原生艾特变量。

## 1.5.1 - 2026-08-19

- 缩短群内设置按钮文字，避免 QQ 客户端显示不全。

## 1.5.0 - 2026-08-19

- 将审核模式、UID 条件、AND/OR 和兜底动作加入群内按钮面板。
- 新增全局拒绝关键词和有效 UID 直接通过选项。

## 1.4.0 - 2026-08-19

- 扩展 UID 条件审核和群级配置。
- 修复插件管理页面桥接和旧配置迁移。

## 1.3.0 - 2026-08-19

- 恢复 QQ 原生审核按钮发送。
- 完善多群 WebUI 管理页面和按钮互动兼容桥。

## 1.2.0 - 2026-08-19

- 新增群内绑定、真实群名称读取和独立插件管理页面。

## 1.1.0 - 2026-08-19

- 新增入群申请按钮审批和 B 站 UID 自动审核。

## 1.0.0 - 2026-08-18

- 首次发布 QQ 官方群聊管理接口、禁言与自动审批策略功能。
